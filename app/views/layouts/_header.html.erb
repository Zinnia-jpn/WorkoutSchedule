<header>
  <ul>
    <% if session[:user_id].present? %>
      <li><%= link_to t("layouts.header.title"), schedule_date_path %></li>
    <% else %>
      <li><%= link_to t("layouts.header.title"), root_path %></li>
    <% end %>
  </ul>
  <ul>
    <% if session[:user_id].present? %>
      <li><%= link_to t("layouts.header.schedule"), schedule_date_path %></li>
      <li><%= link_to t("layouts.header.record"), record_new_path %></li>
      <li><%= link_to t("layouts.header.user"), user_path %></li>
      <li><%= link_to t("layouts.header.logout"), logout_path, method: :post %></li>
    <% else %>
      <li><%= link_to t("layouts.header.signup"), signup_path %></li>
      <li><%= link_to t("layouts.header.login"), login_path %></li>
    <% end %>
  </ul>
</header>
